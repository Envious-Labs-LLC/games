# Copy to a local env file and fill in locally. Real secrets live in
# GCP Secret Manager / macOS Keychain, never committed.
# Example keys a game might use at the Run stage:
#   ITCH_API_KEY=
#   STEAM_BUILD_ACCOUNT=
