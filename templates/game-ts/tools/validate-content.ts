// Validates content/ data files against their Zod schemas (content/schemas/).
// The template ships with no content yet; this passes until content is added.
// When you add content, load every file here and parse it with its schema,
// failing on any mismatch. See .claude/knowledge/walk/content-pipeline.md.

console.log("validate:content — no content files yet (template). Add schemas in content/schemas/ and wire them here.");
process.exit(0);
